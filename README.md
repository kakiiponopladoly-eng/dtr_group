"# DTR_GROUP"  
"# DTR_GROUP"  
"# DTR_GROUP"  git init git add README.md git commit -m "premier commit" git branch -M main git remote add origin https://github.com/kakiiponopladoly-eng/DTR_GROUP.git git push -u origin main
